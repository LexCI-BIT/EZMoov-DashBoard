# Copy to .env and fill in. Both values are safe to expose in the browser bundle
# ONLY because they are the publishable/anon key — never put the service_role key here.
VITE_SUPABASE_URL=https://<your-project-ref>.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxx
